# 🔧 Nginx WebSocket Fix Configuration
# Add this to your existing server block or create a new file

# WebSocket-specific location for socket.io
location ~* ^/socket.io/ {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    
    # Critical WebSocket headers
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    
    # Standard proxy headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;
    
    # WebSocket timeouts
    proxy_connect_timeout 7d;
    proxy_send_timeout 7d;
    proxy_read_timeout 7d;
    
    # Disable buffering for real-time communication
    proxy_buffering off;
    proxy_request_buffering off;
    
    # Handle connection close properly
    proxy_ignore_client_abort off;
}

# Alternative: Add these directives to your existing location block
# If you want to modify the existing configuration, add these to your main location / block:

# In your existing location / { } block, add:
# proxy_set_header Upgrade $http_upgrade;
# proxy_set_header Connection $connection_upgrade;

# And add this map directive at the http level (outside server blocks):
# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     '' close;
# }